{
  "name": "pinia-tasks",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "server": "json-server -w ./data/db.json",
    "serve": "concurrently \"yarn dev\" \"yarn server\""
  },
  "dependencies": {
    "pinia": "^2.0.23",
    "vue": "^3.2.38"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.3",
    "vite": "^3.0.9",
    "concurrently": "^7.4.0",
    "json-server": "^0.17.0"
  }
}
